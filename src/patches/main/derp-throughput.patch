diff --git a/cmd/derper/depaware.txt b/cmd/derper/depaware.txt
index 9797da623..d1155f156 100644
--- a/cmd/derper/depaware.txt
+++ b/cmd/derper/depaware.txt
@@ -3,7 +3,6 @@ tailscale.com/cmd/derper dependencies: (generated by github.com/tailscale/depawa
      💣 crypto/internal/entropy/v1.0.0                               from crypto/internal/fips140/drbg
         filippo.io/edwards25519                                      from github.com/hdevalence/ed25519consensus
         filippo.io/edwards25519/field                                from filippo.io/edwards25519
-        github.com/axiomhq/hyperloglog                               from tailscale.com/derp/derpserver
         github.com/beorn7/perks/quantile                             from github.com/prometheus/client_golang/prometheus
      💣 github.com/cespare/xxhash/v2                                 from github.com/prometheus/client_golang/prometheus
         github.com/coder/websocket                                   from tailscale.com/derp/derpserver+
@@ -11,7 +10,6 @@ tailscale.com/cmd/derper dependencies: (generated by github.com/tailscale/depawa
         github.com/coder/websocket/internal/util                     from github.com/coder/websocket
         github.com/creachadair/msync/throttle                        from github.com/tailscale/setec/client/setec
    W 💣 github.com/dblohm7/wingoes                                   from tailscale.com/util/winutil
-        github.com/dgryski/go-metro                                  from github.com/axiomhq/hyperloglog
         github.com/fxamacker/cbor/v2                                 from tailscale.com/tka
         github.com/go-json-experiment/json                           from tailscale.com/types/opt+
         github.com/go-json-experiment/json/jsontext                  from tailscale.com/drive+
diff --git a/derp/derpserver/derpserver.go b/derp/derpserver/derpserver.go
index 672e32c4b..1cd7e8004 100644
--- a/derp/derpserver/derpserver.go
+++ b/derp/derpserver/derpserver.go
@@ -37,7 +37,6 @@
 	"sync/atomic"
 	"time"
 
-	"github.com/axiomhq/hyperloglog"
 	"github.com/go4org/hashtriemap"
 	"go4.org/mem"
 	"golang.org/x/sync/errgroup"
@@ -362,7 +361,8 @@ type PacketForwarder interface {
 var packetsDropped = metrics.NewMultiLabelMap[dropReasonKindLabels](
 	"derp_packets_dropped",
 	"counter",
-	"DERP packets dropped by reason and by kind")
+	"DERP packets dropped by reason and by kind",
+)
 
 var bytesDropped = metrics.NewMultiLabelMap[dropReasonKindLabels](
 	"derp_bytes_dropped",
@@ -1136,7 +1136,9 @@ func (c *sclient) run(ctx context.Context) error {
 
 	for {
 		ft, fl, err := derp.ReadFrameHeader(c.br)
-		c.debugLogf("read frame type %d len %d err %v", ft, fl, err)
+		if c.debug {
+			c.logf("read frame type %d len %d err %v", ft, fl, err)
+		}
 		if err != nil {
 			if errors.Is(err, io.EOF) {
 				c.debugLogf("read EOF")
@@ -1296,7 +1298,9 @@ func (c *sclient) handleFrameForwardPacket(_ derp.FrameType, fl uint32) error {
 		return nil
 	}
 
-	dst.debugLogf("received forwarded packet from %s via %s", srcKey.ShortString(), c.key.ShortString())
+	if dst.debug {
+		dst.logf("received forwarded packet from %s via %s", srcKey.ShortString(), c.key.ShortString())
+	}
 
 	return c.sendPkt(dst, pkt{
 		bs:         contents,
@@ -1354,10 +1358,8 @@ func (c *sclient) handleFrameSendPacket(_ derp.FrameType, fl uint32) error {
 		if fwd != nil {
 			s.packetsForwardedOut.Add(1)
 			err := fwd.ForwardPacket(c.key, dstKey, contents)
-			c.debugLogf("SendPacket for %s, forwarding via %s: %v", dstKey.ShortString(), fwd, err)
-			if err != nil {
-				// TODO:
-				return nil
+			if c.debug {
+				c.logf("SendPacket for %s, forwarding via %s: %v", dstKey.ShortString(), fwd, err)
 			}
 			return nil
 		}
@@ -1368,10 +1370,14 @@ func (c *sclient) handleFrameSendPacket(_ derp.FrameType, fl uint32) error {
 			c.requestPeerGoneWriteLimited(dstKey, contents, derp.PeerGoneReasonNotHere)
 		}
 		s.recordDrop(contents, c.key, dstKey, reason)
-		c.debugLogf("SendPacket for %s, dropping with reason=%s", dstKey.ShortString(), reason)
+		if c.debug {
+			c.logf("SendPacket for %s, dropping with reason=%s", dstKey.ShortString(), reason)
+		}
 		return nil
 	}
-	c.debugLogf("SendPacket for %s, sending directly", dstKey.ShortString())
+	if c.debug {
+		c.logf("SendPacket for %s, sending directly", dstKey.ShortString())
+	}
 
 	p := pkt{
 		bs:         contents,
@@ -1497,7 +1503,7 @@ func (s *Server) recordDrop(packetBytes []byte, srcKey, dstKey key.NodePublic, r
 		// limiter acts based on the format string, and we want to
 		// rate-limit per src/dst keys, not on the generic "dropped
 		// stuff" message.
-		msg := fmt.Sprintf("drop (%s) %s -> %s", srcKey.ShortString(), reason, dstKey.ShortString())
+		msg := fmt.Sprintf("drop (%s) %s -> %s", reason, srcKey.ShortString(), dstKey.ShortString())
 		s.limitedLogf(msg)
 	}
 	s.debugLogf("dropping packet reason=%s dst=%s disco=%v", reason, dstKey, looksDisco)
@@ -1517,21 +1523,25 @@ func (c *sclient) sendPkt(dst *sclient, p pkt) error {
 	for attempt := range 3 {
 		select {
 		case <-dst.ctx.Done():
-			s.recordDrop(p.bs, c.key, dstKey, dropReasonGoneDisconnected)
-			dst.debugLogf("sendPkt attempt %d dropped, dst gone", attempt)
+			s.recordDrop(p.bs, p.src, dstKey, dropReasonGoneDisconnected)
+			if dst.debug {
+				dst.logf("sendPkt attempt %d dropped, dst gone", attempt)
+			}
 			return nil
 		default:
 		}
 		select {
 		case sendQueue <- p:
-			dst.debugLogf("sendPkt attempt %d enqueued", attempt)
+			if dst.debug {
+				dst.logf("sendPkt attempt %d enqueued", attempt)
+			}
 			return nil
 		default:
 		}
 
 		select {
 		case pkt := <-sendQueue:
-			s.recordDrop(pkt.bs, c.key, dstKey, dropReasonQueueHead)
+			s.recordDrop(pkt.bs, pkt.src, dstKey, dropReasonQueueHead)
 			c.recordQueueTime(pkt.enqueuedAt)
 		default:
 		}
@@ -1539,8 +1549,8 @@ func (c *sclient) sendPkt(dst *sclient, p pkt) error {
 	// Failed to make room for packet. This can happen in a heavily
 	// contended queue with racing writers. Give up and tail-drop in
 	// this case to keep reader unblocked.
-	s.recordDrop(p.bs, c.key, dstKey, dropReasonQueueTail)
-	dst.debugLogf("sendPkt attempt %d dropped, queue full")
+	s.recordDrop(p.bs, p.src, dstKey, dropReasonQueueTail)
+	dst.debugLogf("sendPkt dropped after 3 attempts, queue full")
 
 	return nil
 }
@@ -1875,11 +1885,7 @@ type sclient struct {
 	sawSrc map[key.NodePublic]set.Handle
 	bw     *lazyBufioWriter
 
-	// senderCardinality estimates the number of unique peers that have
-	// sent packets to this client. Owned by sendLoop, protected by
-	// senderCardinalityMu for reads from other goroutines.
-	senderCardinalityMu sync.Mutex
-	senderCardinality   *hyperloglog.Sketch
+	uniqueSenders atomic.Uint64
 
 	// Guarded by s.mu
 	//
@@ -1986,7 +1992,7 @@ func expMovingAverage(prev, newValue, alpha float64) float64 {
 
 // recordQueueTime updates the average queue duration metric after a packet has been sent.
 func (c *sclient) recordQueueTime(enqueuedAt time.Time) {
-	elapsed := float64(c.s.clock.Since(enqueuedAt).Milliseconds())
+	elapsed := float64(c.s.clock.Since(enqueuedAt)) / float64(time.Millisecond)
 	for {
 		old := atomic.LoadUint64(c.s.avgQueueDuration)
 		newAvg := expMovingAverage(math.Float64frombits(old), elapsed, 0.1)
@@ -2021,14 +2027,11 @@ func (c *sclient) onSendLoopDone() {
 			return
 		}
 	}
-
 }
 
 func (c *sclient) sendLoop(ctx context.Context) error {
 	defer c.onSendLoopDone()
 
-	c.senderCardinality = hyperloglog.New()
-
 	jitter := rand.N(5 * time.Second)
 	keepAliveTick, keepAliveTickChannel := c.s.clock.NewTicker(derp.KeepAlive + jitter)
 	defer keepAliveTick.Stop()
@@ -2046,6 +2049,9 @@ func (c *sclient) sendLoop(ctx context.Context) error {
 		case <-ctx.Done():
 			return nil
 		case msg := <-c.peerGone:
+			if msg.reason == derp.PeerGoneReasonDisconnected {
+				c.sawSrc[msg.peer] = set.Handle{}
+			}
 			werr = c.sendPeerGone(msg.peer, msg.reason)
 			continue
 		case <-c.meshUpdate:
@@ -2082,6 +2088,9 @@ func (c *sclient) sendLoop(ctx context.Context) error {
 		case <-ctx.Done():
 			return nil
 		case msg := <-c.peerGone:
+			if msg.reason == derp.PeerGoneReasonDisconnected {
+				c.sawSrc[msg.peer] = set.Handle{}
+			}
 			werr = c.sendPeerGone(msg.peer, msg.reason)
 		case <-c.meshUpdate:
 			werr = c.sendMeshUpdates()
@@ -2248,7 +2257,9 @@ func (c *sclient) sendPacket(srcKey key.NodePublic, contents []byte) (err error)
 			c.s.packetsSent.Add(1)
 			c.s.bytesSent.Add(int64(len(contents)))
 		}
-		c.debugLogf("sendPacket from %s: %v", srcKey.ShortString(), err)
+		if c.debug {
+			c.logf("sendPacket from %s: %v", srcKey.ShortString(), err)
+		}
 	}()
 
 	c.setWriteDeadline()
@@ -2258,11 +2269,6 @@ func (c *sclient) sendPacket(srcKey key.NodePublic, contents []byte) (err error)
 	if withKey {
 		pktLen += key.NodePublicRawLen
 		c.noteSendFromSrc(srcKey)
-		if c.senderCardinality != nil {
-			c.senderCardinalityMu.Lock()
-			c.senderCardinality.Insert(srcKey.AppendTo(nil))
-			c.senderCardinalityMu.Unlock()
-		}
 	}
 	if err = derp.WriteFrameHeader(c.bw.bw(), derp.FrameRecvPacket, uint32(pktLen)); err != nil {
 		return err
@@ -2276,27 +2282,20 @@ func (c *sclient) sendPacket(srcKey key.NodePublic, contents []byte) (err error)
 	return err
 }
 
-// EstimatedUniqueSenders returns an estimate of the number of unique peers
-// that have sent packets to this client.
-func (c *sclient) EstimatedUniqueSenders() uint64 {
-	c.senderCardinalityMu.Lock()
-	defer c.senderCardinalityMu.Unlock()
-	if c.senderCardinality == nil {
-		return 0
-	}
-	return c.senderCardinality.Estimate()
-}
-
 // noteSendFromSrc notes that we are about to write a packet
 // from src to sclient.
 //
 // It must only be called from the sendLoop goroutine.
 func (c *sclient) noteSendFromSrc(src key.NodePublic) {
-	if _, ok := c.sawSrc[src]; ok {
+	h, seen := c.sawSrc[src]
+	if h != (set.Handle{}) {
 		return
 	}
-	h := c.s.addPeerGoneFromRegionWatcher(src, c.onPeerGoneFromRegion)
+	h = c.s.addPeerGoneFromRegionWatcher(src, c.onPeerGoneFromRegion)
 	mak.Set(&c.sawSrc, src, h)
+	if !seen {
+		c.uniqueSenders.Add(1)
+	}
 }
 
 // AddPacketForwarder registers fwd as a packet forwarder for dst.
@@ -2638,7 +2637,7 @@ func (s *Server) ServeDebugTraffic(w http.ResponseWriter, r *http.Request) {
 					next.Key = pkey
 					if cs, ok := s.clients.Load(pkey); ok {
 						if c := cs.activeClient.Load(); c != nil {
-							next.UniqueSenders = c.EstimatedUniqueSenders()
+							next.UniqueSenders = c.uniqueSenders.Load()
 						}
 					}
 					if err := enc.Encode(next); err != nil {
@@ -2662,7 +2661,7 @@ func (s *Server) ServeDebugTraffic(w http.ResponseWriter, r *http.Request) {
 
 var bufioWriterPool = &sync.Pool{
 	New: func() any {
-		return bufio.NewWriterSize(io.Discard, 2<<10)
+		return bufio.NewWriterSize(io.Discard, 16<<10)
 	},
 }
 
diff --git a/derp/derpserver/derpserver_test.go b/derp/derpserver/derpserver_test.go
index b2b5e25bb..3c4542811 100644
--- a/derp/derpserver/derpserver_test.go
+++ b/derp/derpserver/derpserver_test.go
@@ -11,12 +11,12 @@
 	"crypto/tls"
 	"crypto/x509"
 	"encoding/asn1"
-	"encoding/binary"
 	"encoding/json"
 	"expvar"
 	"fmt"
 	"log"
 	"net"
+	"net/http/httptest"
 	"net/netip"
 	"os"
 	"path/filepath"
@@ -28,12 +28,13 @@
 	"testing/synctest"
 	"time"
 
-	"github.com/axiomhq/hyperloglog"
 	qt "github.com/frankban/quicktest"
 	"go4.org/mem"
 	"golang.org/x/time/rate"
 	"tailscale.com/derp"
 	"tailscale.com/derp/derpconst"
+	"tailscale.com/derp/derphttp"
+	"tailscale.com/net/netmon"
 	"tailscale.com/tstime"
 	"tailscale.com/types/key"
 	"tailscale.com/types/logger"
@@ -206,6 +207,7 @@ func TestRecvClientKeyAppName(t *testing.T) {
 func (testFwd) ForwardPacket(key.NodePublic, key.NodePublic, []byte) error {
 	panic("not called in tests")
 }
+
 func (testFwd) String() string {
 	panic("not called in tests")
 }
@@ -341,7 +343,7 @@ type channelFwd struct {
 }
 
 func (f channelFwd) String() string { return "" }
-func (f channelFwd) ForwardPacket(_ key.NodePublic, _ key.NodePublic, packet []byte) error {
+func (f channelFwd) ForwardPacket(_, _ key.NodePublic, packet []byte) error {
 	f.c <- packet
 	return nil
 }
@@ -402,6 +404,7 @@ func TestMultiForwarder(t *testing.T) {
 		t.Errorf("expected %d messages to be forwarded; got %d", numMsgs, received)
 	}
 }
+
 func TestMetaCert(t *testing.T) {
 	priv := key.NewNode()
 	pub := priv.Public()
@@ -892,35 +895,6 @@ func TestParseSSOutput(t *testing.T) {
 	}
 }
 
-func TestServeDebugTrafficUniqueSenders(t *testing.T) {
-	s := New(key.NewNode(), t.Logf)
-	defer s.Close()
-
-	clientKey := key.NewNode().Public()
-	c := &sclient{
-		key:               clientKey,
-		s:                 s,
-		logf:              logger.Discard,
-		senderCardinality: hyperloglog.New(),
-	}
-
-	for range 5 {
-		c.senderCardinality.Insert(key.NewNode().Public().AppendTo(nil))
-	}
-
-	s.mu.Lock()
-	cs := &clientSet{}
-	cs.activeClient.Store(c)
-	s.clients.Store(clientKey, cs)
-	s.mu.Unlock()
-
-	estimate := c.EstimatedUniqueSenders()
-	t.Logf("Estimated unique senders: %d", estimate)
-	if estimate < 4 || estimate > 6 {
-		t.Errorf("EstimatedUniqueSenders() = %d, want ~5 (4-6 range)", estimate)
-	}
-}
-
 func TestGetPerClientSendQueueDepth(t *testing.T) {
 	c := qt.New(t)
 	envKey := "TS_DEBUG_DERP_PER_CLIENT_SEND_QUEUE_DEPTH"
@@ -947,146 +921,20 @@ func TestGetPerClientSendQueueDepth(t *testing.T) {
 	}
 }
 
-func TestSenderCardinality(t *testing.T) {
-	s := New(key.NewNode(), t.Logf)
-	defer s.Close()
-
-	c := &sclient{
-		key:  key.NewNode().Public(),
-		s:    s,
-		logf: logger.WithPrefix(t.Logf, "test client: "),
-	}
-
-	if got := c.EstimatedUniqueSenders(); got != 0 {
-		t.Errorf("EstimatedUniqueSenders() before init = %d, want 0", got)
-	}
-
-	c.senderCardinality = hyperloglog.New()
-
-	if got := c.EstimatedUniqueSenders(); got != 0 {
-		t.Errorf("EstimatedUniqueSenders() with no senders = %d, want 0", got)
-	}
-
-	senders := make([]key.NodePublic, 10)
-	for i := range senders {
-		senders[i] = key.NewNode().Public()
-		c.senderCardinality.Insert(senders[i].AppendTo(nil))
-	}
-
-	estimate := c.EstimatedUniqueSenders()
-	t.Logf("Estimated unique senders after 10 inserts: %d", estimate)
-
-	if estimate < 8 || estimate > 12 {
-		t.Errorf("EstimatedUniqueSenders() = %d, want ~10 (8-12 range)", estimate)
-	}
-
-	for i := range 5 {
-		c.senderCardinality.Insert(senders[i].AppendTo(nil))
-	}
-
-	estimate2 := c.EstimatedUniqueSenders()
-	t.Logf("Estimated unique senders after duplicates: %d", estimate2)
-
-	if estimate2 < 8 || estimate2 > 12 {
-		t.Errorf("EstimatedUniqueSenders() after duplicates = %d, want ~10 (8-12 range)", estimate2)
-	}
-}
-
-func TestSenderCardinality100(t *testing.T) {
-	s := New(key.NewNode(), t.Logf)
-	defer s.Close()
-
-	c := &sclient{
-		key:               key.NewNode().Public(),
-		s:                 s,
-		logf:              logger.WithPrefix(t.Logf, "test client: "),
-		senderCardinality: hyperloglog.New(),
-	}
-
-	numSenders := 100
-	for range numSenders {
-		c.senderCardinality.Insert(key.NewNode().Public().AppendTo(nil))
-	}
-
-	estimate := c.EstimatedUniqueSenders()
-	t.Logf("Estimated unique senders for 100 actual senders: %d", estimate)
-
-	if estimate < 85 || estimate > 115 {
-		t.Errorf("EstimatedUniqueSenders() = %d, want ~100 (85-115 range)", estimate)
-	}
-}
-
-func TestSenderCardinalityTracking(t *testing.T) {
+func TestUniqueSenders(t *testing.T) {
 	s := New(key.NewNode(), t.Logf)
 	defer s.Close()
-
-	c := &sclient{
-		key:               key.NewNode().Public(),
-		s:                 s,
-		logf:              logger.WithPrefix(t.Logf, "test client: "),
-		senderCardinality: hyperloglog.New(),
-	}
-
-	zeroKey := key.NodePublic{}
-	if zeroKey != (key.NodePublic{}) {
-		c.senderCardinality.Insert(zeroKey.AppendTo(nil))
-	}
-
-	if estimate := c.EstimatedUniqueSenders(); estimate != 0 {
-		t.Errorf("EstimatedUniqueSenders() after zero key = %d, want 0", estimate)
-	}
-
-	sender1 := key.NewNode().Public()
-	sender2 := key.NewNode().Public()
-
-	if sender1 != (key.NodePublic{}) {
-		c.senderCardinality.Insert(sender1.AppendTo(nil))
-	}
-	if sender2 != (key.NodePublic{}) {
-		c.senderCardinality.Insert(sender2.AppendTo(nil))
-	}
-
-	estimate := c.EstimatedUniqueSenders()
-	t.Logf("Estimated unique senders after 2 senders: %d", estimate)
-
-	if estimate < 1 || estimate > 3 {
-		t.Errorf("EstimatedUniqueSenders() = %d, want ~2 (1-3 range)", estimate)
-	}
-}
-
-func BenchmarkHyperLogLogInsert(b *testing.B) {
-	hll := hyperloglog.New()
-	sender := key.NewNode().Public()
-	senderBytes := sender.AppendTo(nil)
-
-	b.ResetTimer()
-	for i := 0; i < b.N; i++ {
-		hll.Insert(senderBytes)
-	}
-}
-
-func BenchmarkHyperLogLogInsertUnique(b *testing.B) {
-	hll := hyperloglog.New()
-
-	b.ResetTimer()
-
-	buf := make([]byte, 32)
-	for i := 0; i < b.N; i++ {
-		binary.LittleEndian.PutUint64(buf, uint64(i))
-		hll.Insert(buf)
-	}
-}
-
-func BenchmarkHyperLogLogEstimate(b *testing.B) {
-	hll := hyperloglog.New()
-
-	for range 100 {
-		hll.Insert(key.NewNode().Public().AppendTo(nil))
-	}
-
-	b.ResetTimer()
-	for i := 0; i < b.N; i++ {
-		_ = hll.Estimate()
+	c := &sclient{s: s}
+	if got := c.uniqueSenders.Load(); got != 0 {
+		t.Fatalf("initial unique senders = %d, want 0", got)
+	}
+	for want := uint64(1); want <= 100; want++ {
+		sender := key.NewNode().Public()
+		c.noteSendFromSrc(sender)
+		c.noteSendFromSrc(sender)
+		if got := c.uniqueSenders.Load(); got != want {
+			t.Fatalf("unique senders = %d, want %d", got, want)
+		}
 	}
 }
 
@@ -1439,7 +1287,7 @@ func TestLoadRateConfig(t *testing.T) {
 	} {
 		t.Run(tt.name, func(t *testing.T) {
 			f := filepath.Join(t.TempDir(), "rate.json")
-			if err := os.WriteFile(f, []byte(tt.json), 0644); err != nil {
+			if err := os.WriteFile(f, []byte(tt.json), 0o644); err != nil {
 				t.Fatal(err)
 			}
 			rc, err := LoadRateConfig(f)
@@ -1465,7 +1313,7 @@ func TestLoadRateConfig(t *testing.T) {
 			path := tt.path
 			if tt.content != "" {
 				path = filepath.Join(t.TempDir(), "rate.json")
-				if err := os.WriteFile(path, []byte(tt.content), 0644); err != nil {
+				if err := os.WriteFile(path, []byte(tt.content), 0o644); err != nil {
 					t.Fatal(err)
 				}
 			}
@@ -1481,7 +1329,7 @@ func TestLoadAndApplyRateConfig(t *testing.T) {
 	writeConfig := func(t *testing.T, json string) string {
 		t.Helper()
 		f := filepath.Join(t.TempDir(), "rate.json")
-		if err := os.WriteFile(f, []byte(json), 0644); err != nil {
+		if err := os.WriteFile(f, []byte(json), 0o644); err != nil {
 			t.Fatal(err)
 		}
 		return f
@@ -1555,7 +1403,7 @@ func TestLoadAndApplyRateConfig(t *testing.T) {
 		}
 		s.mu.Unlock()
 
-		if err := os.WriteFile(f, []byte(`{}`), 0644); err != nil {
+		if err := os.WriteFile(f, []byte(`{}`), 0o644); err != nil {
 			t.Fatal(err)
 		}
 		if err := s.LoadAndApplyRateConfig(f); err != nil {
@@ -1715,23 +1563,53 @@ func BenchmarkLookupDestHashTrie(b *testing.B) {
 	})
 }
 
-func BenchmarkSenderCardinalityOverhead(b *testing.B) {
-	hll := hyperloglog.New()
-	sender := key.NewNode().Public()
-
-	b.Run("WithTracking", func(b *testing.B) {
-		b.ReportAllocs()
-		for i := 0; i < b.N; i++ {
-			if hll != nil {
-				hll.Insert(sender.AppendTo(nil))
-			}
+func TestRepeatedPeerGone(t *testing.T) {
+	s := New(key.NewNode(), t.Logf)
+	defer s.Close()
+	hs := httptest.NewServer(Handler(s))
+	defer hs.Close()
+	connect := func(priv key.NodePrivate) *derphttp.Client {
+		c, err := derphttp.NewClient(priv, hs.URL+"/derp", logger.Discard, netmon.NewStatic())
+		if err != nil {
+			t.Fatal(err)
 		}
-	})
-
-	b.Run("WithoutTracking", func(b *testing.B) {
-		b.ReportAllocs()
-		for i := 0; i < b.N; i++ {
-			_ = sender.AppendTo(nil)
+		t.Cleanup(func() { c.Close() })
+		if err := c.Connect(t.Context()); err != nil {
+			t.Fatal(err)
 		}
-	})
+		if _, err := c.Recv(); err != nil {
+			t.Fatal(err)
+		}
+		return c
+	}
+	receiver := connect(key.NewNode())
+	timeout := time.AfterFunc(2*time.Second, func() { receiver.Close() })
+	defer timeout.Stop()
+	senderKey := key.NewNode()
+	for attempt := range 3 {
+		sender := connect(senderKey)
+		if err := sender.Send(receiver.SelfPublicKey(), []byte("hello")); err != nil {
+			t.Fatal(err)
+		}
+		if packet, err := receiver.Recv(); err != nil {
+			t.Fatal(err)
+		} else if _, ok := packet.(derp.ReceivedPacket); !ok {
+			t.Fatalf("packet is %T", packet)
+		}
+		sender.Close()
+		msg, err := receiver.Recv()
+		if err != nil {
+			t.Fatalf("disconnect %d: %v", attempt+1, err)
+		}
+		if gone, ok := msg.(derp.PeerGoneMessage); !ok || gone.Peer != senderKey.Public() || gone.Reason != derp.PeerGoneReasonDisconnected {
+			t.Fatalf("disconnect %d: got %#v", attempt+1, msg)
+		}
+	}
+	cs, ok := s.clients.Load(receiver.SelfPublicKey())
+	if !ok {
+		t.Fatal("receiver not registered")
+	}
+	if got := cs.activeClient.Load().uniqueSenders.Load(); got != 1 {
+		t.Fatalf("unique senders after reconnects = %d, want 1", got)
+	}
 }
